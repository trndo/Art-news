{% extends 'admin/admin_base.html.twig' %}
{% set selected = 'pictures' %}
{% block content %}
    <div class="card">
        <div class="card-header card-header-primary">
            <h2 class="card-title">Створення малюнка</h2>
            <h4 class="category">При створенні малюнка, мова за замовчуванням - українська! Додати англійську ви можете в меню всіх малюнків!</h4>
        </div>
        <br>
        <div class="container">
            <div class="row">
                <div class="col-12">
                    {{ form_start(form) }}
                    <div class="form-group form-file-upload form-file-multiple">
                        <br>
                        <img src="/uploads/pictures/{{ picture.photo }}" alt="article" style="object-fit: contain" width="250px" height="250px">
                        {{ form_row(form.photo,{
                            'attr': {
                                'class': 'form-control inputFileVisible'
                            },
                            'label' : 'Титульна фотографія'
                        }) }}
                    </div>
                    {% set counter = 0 %}
                    {% for photo in form.photos %}
                        <img src="/uploads/pictures/{{ picture.photos[counter].photo }}" alt="article" style="object-fit: contain" width="250px" height="250px">
                            {{ form_widget(photo.photo) }}
                        <br>
                        {% set counter = counter + 1 %}
                    {% endfor %}
                    <div class="form-group">
                        {{ form_row(form.save, {
                            'attr': {
                                'class': 'btn btn-primary'
                            }
                        }) }}
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}